package com.bluelight.backend.domain.application;

/**
 * 신청 유형: 신규(NEW) / 갱신(RENEWAL) / 임시 전기 공급(SUPPLY_INSTALLATION)
 */
public enum ApplicationType {
    NEW,
    RENEWAL,
    SUPPLY_INSTALLATION
}
