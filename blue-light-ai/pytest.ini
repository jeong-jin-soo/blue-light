[pytest]
markers =
    vision: Vision AI tests (require GEMINI_API_KEY, incur API costs)
    slow: Slow tests (skip in CI fast path)
    snapshot: SVG snapshot tests (platform-dependent font rendering, local only)
